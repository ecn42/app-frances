[build]
builder = "RAILPACK"

[deploy]
startCommand = "python scripts/bootstrap_db.py && python run.py"
healthcheckPath = "/_stcore/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
